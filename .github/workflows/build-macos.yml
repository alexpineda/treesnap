name: Build Tauri App (macOS)

on:
  push:
    branches:
      - release
  workflow_dispatch: # Manual trigger

jobs:
  build-macos:
    permissions:
      contents: write
    strategy:
      matrix:
        target: ["aarch64-apple-darwin", "x86_64-apple-darwin"]
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Bun
        run: curl -fsSL https://bun.sh/install | bash -s -- "bun-v1.2.6" # Pin a version for consistency
      - name: Add Bun to PATH
        run: echo "$HOME/.bun/bin" >> $GITHUB_PATH
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - name: Install frontend dependencies
        run: bun install
      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__
          releaseName: "RepoSnap v__VERSION__ (macOS ${{ matrix.target }})"
          releaseBody: "Unsigned macOS build for ${{ matrix.target }} (.dmg)."
          releaseDraft: true
          args: "--target ${{ matrix.target }} --bundles dmg"
